language:
    - cpp
compiler:
    - clang
    - gcc
dist:
    - trusty
cache:
    directories:
    - OpenBLAS
before_install:
    - sudo apt-get update -qq -y
    - sudo apt-get install -y gfortran libopenblas-dev
script:
    - make test
